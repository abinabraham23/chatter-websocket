apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eap72-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: eap72-test.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eap72
                port:
                  number: 8080